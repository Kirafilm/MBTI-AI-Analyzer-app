# 2026-06-13

## 測試帳號登入修復
- **問題**：測試帳號登入後立即被踢出。原因：`Api.getMe()` 調用 localhost:3000 後端失敗，`useAuth.fetchUser()` 清除 session token
- **修復 1**：`getMe()` 加 try-catch，後端不可達時回 `null` 而非拋異常
- **修復 2**：測試帳號登入加入 fast path，完全跳過 Supabase（網路不可達時也能用）
- **修復 3**：`useAuth` 中 test premium override 激活時，建立 fallback user 保留 session

## 代碼質量提升
- 清理 4 處空 catch 塊（ad-gate.native.ts、use-auth.ts），加 console.warn
- Premium 狀態架構已確認正確：Context 是 single source of truth，AsyncStorage 只做持久化
- 發現 root level `psychology-tests-list.tsx` 是死代碼（未被任何 route 引用）

## 項目架構關鍵點
- Auth：Supabase（session token）+ 後端 API（localhost:3000，驗證 token）+ RevenueCat（付費）
- 後端 API 不可選：測試帳號走 fake token + test premium override
- Premium：前 3 個心理測驗免費（含廣告），解鎖全部需一次性付費
- 6 個心理測驗：焦慮、快樂、壓力、抑鬱、睡眠、專注

## Supabase 暫停恢復
- Supabase 免費專案曾暫停（7天不活動自動 pause），用戶已手動恢復
- register.tsx 加了詳細錯誤訊息（顯示 Supabase 原始 error.message），方便未來排查

## 忘記密碼 & 重設密碼
- login.tsx 加了忘記密碼 Modal（底部 sheet 風格），輸入 email 發 Supabase resetPasswordForEmail
- 建立 `/auth/reset-password` 頁面，處理郵件連結的 `#access_token=xxx&type=recovery`
- reset-password 頁面加載時用 `supabase.auth.setSession()` 建立臨時 session（之前沒做這步導致「Auth session missing!」）

## Premium 泄漏修復
- `setTestPremiumOverride(false)` 加了清除 `PREMIUM_CACHE_KEY`，避免 premium 緩存泄漏到下一個 session

## 購買頁面
- 建立 `/app/(tabs)/purchase.tsx`，獨立購買頁面（功能列表 + 價格 + 購買/恢復按鈕）
- Tab bar 新增「解鎖」tab（lock.open.fill icon）
- psychology-list 的 locked 按鈕改為跳轉購買頁面（取代 Alert 彈窗）
- 三語翻譯已添加（purchaseTitle, premiumFeatures, noAds 等 20+ keys）
